

//在需要權限的地方
if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.M) {
            int per1 = checkSelfPermission(Manifest.permission.WRITE_EXTERNAL_STORAGE);
            MLog.w("PER", "per=" + per1);
            if (per1 != PackageManager.PERMISSION_GRANTED)
                requestPermissions(new String[]{Manifest.permission.WRITE_EXTERNAL_STORAGE}, 2);

        }
		
		
		  @Override
    public void onRequestPermissionsResult(int requestCode, @NonNull String[] permissions, @NonNull int[] grantResults) {

        MLog.w("what", "grantResults length=" + grantResults.length);
        MLog.w("what", "grantResults =" + grantResults[0]);
        switch (requestCode) {
            case 1:
            case 2:
                if (grantResults.length > 0
                        && grantResults[0] == PackageManager.PERMISSION_GRANTED) {
                    MLog.w("permissions", "GRANTED");
                    // permission was granted, yay! Do the
                    // contacts-related task you need to do.
                } else {
                    MLog.w("permissions", "denied");
                    // permission denied, boo! Disable the
                    // functionality that depends on this permission.
                }
                break;

        }
    }