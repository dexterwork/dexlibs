package dexter.studio.sqlitemodule.sample;

public class exec_sample {


//    public void onUpgrade(SQLiteDatabase db, int oldVersion, int newVersion) {
////        switch (newVersion) {
////            case 2:
////                String goldCoint = "ALTER TABLE " + getTableNameChannel() + " ADD COLUMN gold_coin INTEGER DEFAULT 0";
////                db.execSQL(goldCoint);
////                String pointCoint = "ALTER TABLE " + getTableNameChannel() + " ADD COLUMN point_coin INTEGER DEFAULT 0";
////                db.execSQL(pointCoint);
////                String coinLock = "ALTER TABLE " + getTableNameChannel() + " ADD COLUMN coin_lock INTEGER DEFAULT 0";
////                db.execSQL(coinLock);
////                String startTime = "ALTER TABLE " + getTableNameChannel() + " ADD COLUMN start_time TEXT";
////                db.execSQL(startTime);
////                String endTime = "ALTER TABLE " + getTableNameChannel() + " ADD COLUMN end_time TEXT";
////                db.execSQL(endTime);
////                break;
////        }
//    }
}
